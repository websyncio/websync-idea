package org.websync.connection.messages.browser;

import org.websync.connection.dto.PageInstanceDto;
import org.websync.connection.messages.ProjectMessage;

public class PageInstanceMessage extends ProjectMessage {
    public PageInstanceDto pageInstance;
}
